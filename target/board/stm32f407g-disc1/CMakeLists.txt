add_library(BOARD_TARGET INTERFACE)

set(DEVICE STM32F407xx PARENT_SCOPE)
set(TARGET_LINKER_FILE ${CMAKE_CURRENT_SOURCE_DIR}/inc/STM32F407XX_FLASH.ld PARENT_SCOPE)
set(HAL_SYSTEM_SOURCE ${CMAKE_CURRENT_SOURCE_DIR}/src/system_stm32f4xx.c PARENT_SCOPE)
set(HAL_STARTUP_SOURCE ${CMAKE_CURRENT_SOURCE_DIR}/src/startup_stm32f407xx.s PARENT_SCOPE)

set(HAL_DEFINE_SYMBOLS
        USER_VECT_TAB_ADDRESS
        VECT_TAB_SRAM
        PARENT_SCOPE
)

target_sources(BOARD_TARGET PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/src/board.c
)

target_include_directories(BOARD_TARGET INTERFACE
        ${CMAKE_CURRENT_SOURCE_DIR}/inc
)

target_link_libraries(BOARD_TARGET INTERFACE VENDOR_TARGET)

message(STATUS "board selected : " ${BOARD})